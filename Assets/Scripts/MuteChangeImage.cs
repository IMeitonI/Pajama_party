using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class MuteChangeImage : MonoBehaviour
{
    [SerializeField] Image imagen;
    [SerializeField] Sprite mute, volumeOn; // Start is called before the first frame update
   

    // Update is called once per frame
    void Update()
    {
        
    }
}
